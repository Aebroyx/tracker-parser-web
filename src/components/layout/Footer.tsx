/**
 * Minimal footer.
 */

export function Footer() {
  return (
    <footer className="flex items-center justify-center px-4 sm:px-6 py-4 pb-safe border-t border-border-subtle">
      <p className="text-xs text-text-muted">
        100% client-side · Your data never leaves your browser
      </p>
    </footer>
  );
}
